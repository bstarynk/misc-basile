# Cryptarithmetic puzzles

This `misc-basile/CryptArithm` directory in https://github.com/bstarynk/misc-basile/ has some cryptarithmetic puzzles. See https://en.wikipedia.org/wiki/Verbal_arithmetic

## cryptadditions

In directory `neuf+deux=onze` the puzzle NEUF+DEUX=ONZE
(in French,  `neuf` is 9 (nine), `deux` is 2 (two), and `onze` is 11 (eleven)...)
